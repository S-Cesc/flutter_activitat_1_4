dropdown_menu.dart has an error. The function "search" deals with the searchText.isEmpty case, but not with entries.isEmpty case. The code is


line 618:

  int? search(List<DropdownMenuEntry<T>> entries,
      TextEditingController textEditingController) {
    final String searchText = textEditingController.value.text.toLowerCase();
    if (searchText.isEmpty) {
      return null;
    }
...


line 618, corrected:

  int? search(List<DropdownMenuEntry<T>> entries,
      TextEditingController textEditingController) {
    final String searchText = textEditingController.value.text.toLowerCase();
    if (searchText.isEmpty || entries.isEmpty) {
      return null;
    }
...